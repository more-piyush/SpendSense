# configs/cat_distilbert_base.yaml
# DistilBERT with default/standard hyperparameters
# Purpose: Neural network baseline — standard fine-tuning recipe.

run_name: "cat_distilbert_base"
model_type: distilbert

# Model
pretrained_model: distilbert-base-uncased
max_length: 64

# Training
learning_rate: 0.00002    # 2e-5 (standard BERT fine-tuning rate)
batch_size: 32
epochs: 5
warmup_ratio: 0.1
weight_decay: 0.01
dropout: 0.3
freeze_layers: 0          # No layer freezing
early_stopping_patience: 2
mixed_precision: true

# Loss
loss: bce_with_logits

# Data
data_path: /data/categorization_training.parquet

# MLflow
mlflow_tracking_uri: http://CHAMELEON_FLOATING_IP:5000
experiment_name: categorization
