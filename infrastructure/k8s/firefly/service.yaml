apiVersion: v1
kind: Service
metadata:
  name: firefly
  namespace: firefly-platform
spec:
  type: NodePort
  selector:
    app: firefly
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
