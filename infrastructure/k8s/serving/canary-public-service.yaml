apiVersion: v1
kind: Service
metadata:
  name: serving-canary-public
  namespace: firefly-platform
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: spendsense-serving
    app.kubernetes.io/instance: canary
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30082
