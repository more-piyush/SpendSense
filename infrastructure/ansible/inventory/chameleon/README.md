`hosts.yml` is generated by Terraform from `infrastructure/tf/kvm`.

If you want to manage inventory manually instead, create a file with the same
group layout:

- `control_plane`
- `workers`
- `k8s_cluster`
